#!/usr/bin/env bash

CMD="vendor/bin/php-cs-fixer fix --dry-run --verbose --diff ${@:-src/}"

if [[ $TRAVIS = true ]] ; then
    source .travis/php_no_xdebug
    CMD="php_no_xdebug ${CMD}"
fi

${CMD}
